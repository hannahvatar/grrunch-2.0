# Copy to .env in this scripts/ directory and fill in real values. Never commit
# the filled-in .env — these are server-side secrets (Airtable PAT, Supabase
# service_role key), not the public anon key used by the app itself.
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=appnYzFFuss14JCXP
SUPABASE_URL=https://vzuugbiqbzritdystnar.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
